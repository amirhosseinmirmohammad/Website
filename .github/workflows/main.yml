name: ci

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: amirhosseinmirmohammad17
          password: dckr_pat_ne_b5Mx5LmBSmbL7OUDOA_B-nYM

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: src/Web/Website
          file: src/Web/Website/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: amirhosseinmirmohammad17/website:latest
